import BeatRunnerLoader from './BeatRunnerLoader';

export const metadata = {
    title: 'Beat Runner — Cognigym Test',
    robots: { index: false },
};

export default function CognigymTestPage() {
    return <BeatRunnerLoader />;
}
